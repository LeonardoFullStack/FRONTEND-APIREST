<%- include('./templates/header.ejs') %>
<%- include('nav.ejs') %>

<main>
    <h1><%= titulo%></h1>
    <p><%= msg%></p>

    <a href="/admin/crear/">
        <button>
            crear servicio
        </button>
    </a>
    <% if (data.length>0) { %>
     
    <% data.forEach(element => { %>
     <div class="servicios">
        <h2><%= element.servicio %></h2> <p class="right"><%= element.fecha %></p>
        <p><%= element.descripcion %></p>

        <a href="/admin/uno/<%= element._id %>">
            <button id="<%= element._id %>">
                Leer más
            </button>
        </a>
        <a href="/admin/edit/<%= element._id %>">
            <button id="<%= element._id %>">
                Editar
            </button>
        </a>
        <a href="/admin/eliminar/<%=element._id %>">
            <button id="<%= element._id %>">
                Eliminar
            </button>
        </a>

     </div>
    <% }) %>
    <% } else  { %>
        <div class="servicios">
            <h2><%= data.data.servicio %></h2> <p class="right"><%= data.data.fecha %></p>
            <p><%= data.data.descripcion %></p>
    
    
    
            <a href="/admin/edit/<%= data.data._id %>">
                <button id="<%= data.data._id %>">
                    Editar
                </button>
            </a>
            <a href="/admin/eliminar/<%=data.data._id %>">
                <button id="<%= data.data._id %>">
                    Eliminar
                </button>
            </a>
    
         </div>
        <% } %>
    </main>

<%- include('./templates/footer.ejs') %>
